# NovaCloud 完整链路学习手册

> Kubernetes 部署入口已补充，见 [Kubernetes学习与操作](docs/Kubernetes学习与操作.md)。本文原有端口和 Docker DNS 示例仍以 Compose 为主；Kubernetes 工作台为 28080，Nacos 为 28848，业务代码和登录链路相同。

> 这不是一份“把所有源码重新抄一遍”的文档，而是一份读项目的路线图。
> 我们从浏览器中的一次真实操作出发，沿着请求经过的每一层找配置、找代码、看数据变化，再沿响应原路返回。

## 0. 这份文档怎么使用

当前项目已经实现登录、Redis 会话、两层网关鉴权、权限校验、限流、TraceId、工单查询和浏览器工作台。一次全部记住没有必要，推荐按下面的顺序学习：

1. 先读第 1～2 章，知道系统里有哪些东西、Java 代码从哪里下手。
2. 完整学习第 3 章登录链路，暂时不要同时研究工单查询。
3. 登录链路能讲清楚后，再学习第 5 章带登录状态查询工单。
4. 最后学习权限、TraceId、限流和故障链路。
5. 每学一章，只做该章最后的一个小实验和自检题。

阅读代码时始终问五个问题：

```text
1. 这一层收到了什么？
2. 它先检查什么？
3. 它调用了谁？
4. 数据在哪里发生变化？
5. 成功或失败后，响应交给谁？
```

本文中的“中台”对应项目里的 `platform-*`。有些口述会说成“总台”，本文统一使用代码中的名称“中台层”。

### 当前实现与边界

已经实现并实际验收：

- 浏览器登录、查询工单、退出及错误提示。
- MySQL 演示账号和 BCrypt 密码哈希。
- Redis 30 分钟服务端会话。
- 应用层网关、中台层网关和中台服务的会话及权限检查。
- 登录按来源 IP 限流、工单查询按用户名限流。
- Nginx 生成 TraceId，五层 HTTP 请求日志使用同一编号。
- 两个 Nacos 命名空间内的服务注册与发现。
- 中台服务查询 MySQL 工单表。
- Docker Desktop 单节点 Kubernetes 部署，原链路、登录鉴权、并发限流、日志与浏览器流程复验通过。

尚未实现：

- Kafka 和消息链路追踪。
- JWT、刷新 token、SSO、注册、改密和完整 RBAC 管理后台。
- OpenTelemetry span、采样、集中日志检索平台和可视化链路追踪后台。
- HTTPS 和可直接用于公网生产的安全配置。

实现、故障和验收的时间线以根目录的 [《搭建日志》](搭建日志.md) 为准。本文讲“为什么和怎么读”；现有 `docs/*学习与操作.md` 继续提供专项操作步骤。

---

## 1. 先看懂整个系统

### 1.1 一次请求可能经过哪些组件

```text
浏览器
  │ 访问 http://127.0.0.1:18080
  ▼
Windows 宿主机 18080
  │ Docker 端口映射 18080:80
  ▼
Nginx 容器 80
  │ Docker 内部网络：app-gateway:8080
  ▼
应用层网关 app-gateway
  │ Nacos(application)：lb://app-service
  ▼
应用层服务 app-service
  │ 配置 URL：http://platform-gateway:8080
  ▼
中台层网关 platform-gateway
  │ Nacos(platform)：lb://platform-service
  ▼
中台层服务 platform-service
  ├── MySQL：账号和工单
  └── Redis：登录会话
```

除此之外，应用层网关也会直接访问 Redis，用于校验会话和保存限流计数；中台层网关也会直接访问 Redis 校验会话。

### 1.2 八个长期运行容器分别做什么

| Compose 服务名 | 类型 | 在当前项目中的职责 |
| --- | --- | --- |
| `nginx` | 入口代理、静态资源服务器 | 提供浏览器页面、接收 `/api` 请求、生成 TraceId、转发给应用网关 |
| `app-gateway` | 应用层 Gateway | 会话和权限检查、登录及工单限流、通过 Nacos 路由到 `app-service` |
| `app-service` | 应用层 Java 服务 | 接收应用层请求并转发给中台网关，透传 Authorization 和 TraceId |
| `platform-gateway` | 中台层 Gateway | 再次检查会话和权限，通过 Nacos 路由到 `platform-service` |
| `platform-service` | 中台层 Java 服务 | 校验密码、创建/删除会话、最终权限保护、查询 MySQL |
| `nacos` | 注册中心 | 在两个命名空间中保存服务名和健康实例的对应关系 |
| `redis` | 内存数据存储 | 保存服务端登录会话和限流窗口计数 |
| `mysql` | 关系数据库 | 保存账号、密码哈希、基础权限和工单数据 |

`builder` 也出现在 `compose.yaml` 中，但它只在构建阶段临时运行 Maven，不是长期业务容器。

### 1.3 数据到底保存在哪里

| 数据 | 当前保存位置 | 是否长期业务数据 | 关键位置 |
| --- | --- | --- | --- |
| 用户名 | MySQL `lab_users.username` | 是 | `DemoUsers.java` |
| 密码 | MySQL `lab_users.password_hash`，内容为 BCrypt 哈希 | 是 | `DemoUsers.java` |
| 用户基础权限 | MySQL `lab_users.can_read` | 是 | `DemoUsers.java` |
| 初始演示密码 | 本机 `.env` 的 `DEMO_PASSWORD` | 只是初始化输入 | `scripts/init-env.ps1`、`compose.yaml` |
| 登录 token | 浏览器当前页面的 JavaScript 变量 | 否，刷新即丢失 | `frontend/app.js` |
| 服务端会话 | Redis `lab:session:<token>` | 临时数据，固定 30 分钟 | `SessionStore.java` |
| 会话中的权限 | Redis 会话 JSON | 登录时的权限快照 | `SessionStore.java` |
| 登录限流计数 | Redis `lab:rate:login:<摘要>` | 短期窗口数据 | `RateLimitFilter.java` |
| 工单限流计数 | Redis `lab:rate:tickets:<摘要>` | 短期窗口数据 | `RateLimitFilter.java` |
| 工单 | MySQL `tickets` 表 | 是 | `001-init.sql`、`TicketController.java` |
| TraceId | HTTP 头和容器日志 | 不是业务存储 | `nginx.conf`、请求过滤器 |

最重要的区分是：

```text
MySQL 回答：“这个账号是谁，它的密码哈希和基础权限是什么？”
Redis 回答：“这个 token 目前是否仍然代表一个已登录用户？”
```

### 1.4 项目文件从哪里开始看

```text
NovaCloud/
├── frontend/                 浏览器页面
├── infra/nginx/              Nginx 入口配置
├── app-gateway/              应用层网关
├── app-service/              应用层服务
├── platform-gateway/         中台层网关
├── platform-service/         中台层服务
├── infra/mysql/              MySQL 第一次初始化脚本
├── scripts/                  启动、验收和查日志脚本
├── compose.yaml              整套本地容器环境编排
├── README.md                 运行入口和项目概览
├── 搭建日志.md               已发生的搭建与验收事实
└── 学习文档.md               你正在阅读的主学习手册
```

`compose.yaml` 不属于某一个 Java 服务，也不是容器内部自动生成的文件。它是项目根目录里的环境编排清单，由 Docker Compose 读取，用来创建所有容器、网络、端口映射和数据卷。

---

## 2. 阅读这套 Java 代码前，先认识几个符号

这一章不是完整的 Spring 教程，只解释本项目反复出现的写法。

### 2.1 Controller：真正接住业务 URL 的方法

以中台登录接口为例：

```java
@RestController
@RequestMapping("/api/auth")
class AuthController {
    @PostMapping("/login")
    ResponseEntity<?> login(@RequestBody Credentials credentials) {
        // ...
    }
}
```

逐行翻译：

- `@RestController`：这个类可以接收 HTTP 请求，返回值会写进 HTTP 响应。
- `@RequestMapping("/api/auth")`：这个类里所有接口都有共同前缀 `/api/auth`。
- `@PostMapping("/login")`：这个方法处理 `POST /api/auth/login`。
- `@RequestBody`：把浏览器发送的 JSON 请求体转换成 Java 对象。
- `ResponseEntity<?>`：方法不仅能返回数据，还能决定 HTTP 状态码和响应头。

所以看到一个请求 URL 时，可以先搜索 `@GetMapping`、`@PostMapping` 或 URL 中的关键词。

### 2.2 构造方法参数：对象不是手工 new 出来的

```java
private final DemoUsers users;
private final SessionStore sessions;

AuthController(DemoUsers users, SessionStore sessions) {
    this.users = users;
    this.sessions = sessions;
}
```

这里不是浏览器传入两个参数。Spring 启动时会找到 `DemoUsers` 和 `SessionStore` 对象，再把它们交给 `AuthController`。这叫构造器注入。

读到 `users.authenticate(...)` 时，下一步应打开 `DemoUsers.java`；读到 `sessions.create(...)` 时，下一步应打开 `SessionStore.java`。

### 2.3 Filter：请求到 Controller 之前的检查站

过滤器的核心形状通常是：

```text
收到请求
→ 做检查或补充信息
→ 不通过：直接写响应并结束
→ 通过：chain.doFilter / chain.filter，交给下一层
```

本项目中：

- 两个 Gateway 使用响应式 `WebFilter`。
- 两个普通 MVC 服务使用 `OncePerRequestFilter`。
- `SessionAuth`、`SessionFilter` 做会话和权限检查。
- `RequestLog`、`RequestTraceFilter` 处理 TraceId 和请求日志。

Controller 没有执行，并不一定是 Controller 出错；请求可能已经在过滤器中被 401、403、429 或 503 拦住。

### 2.4 Gateway 为什么出现 `Mono<Void>`

Spring Cloud Gateway 使用 WebFlux 响应式模型。`Mono<Void>` 可以先理解为：

> “这个异步处理完成时没有一个普通 Java 返回对象，但它会完成 HTTP 响应或把请求交给下一环。”

现在不需要先学完整 Reactor。读网关代码时重点看：

- 哪些条件直接 `reject(...)`。
- 哪些条件调用 `chain.filter(...)`。
- Redis 查询完成后，状态码如何决定继续还是拒绝。

### 2.5 `application.yml`：代码之外的路由和地址

Java 类负责“怎么处理”，`application.yml` 经常负责“叫什么、连哪里、哪些 URL 交给谁”。例如：

```yaml
- id: app-tickets
  uri: lb://app-service
  predicates:
    - Path=/api/tickets/**
```

意思是：匹配 `/api/tickets/**` 的请求使用服务名 `app-service` 路由；`lb://` 会结合负载均衡器和 Nacos 服务发现选择健康实例。

如果只搜索 Java Controller，可能看不到网关请求究竟被送到了哪个服务，因此读链路时必须同时看 `application.yml`。

### 2.6 本项目有两种 Java 请求模型

| 类型 | 所在模块 | 常见对象 | TraceId 保存方式 |
| --- | --- | --- | --- |
| 响应式 WebFlux | 两个 Gateway | `ServerWebExchange`、`Mono` | 当前请求的局部变量和请求头，不使用线程 MDC |
| 同步 Servlet MVC | 两个 Service | `HttpServletRequest`、`ResponseEntity` | 当前线程 MDC，请求结束后清理 |

两种写法不同，不代表一边“更高级”。它们只是所用框架和线程模型不同。

---

## 3. 浏览器点击“登录”后，完整发生了什么

这一章只解决一个问题：输入 `demo` 和密码、点击登录以后，token 是怎样产生的？

### 3.1 登录总链路

```text
浏览器 frontend/app.js
  │ POST /api/auth/login
  │ {username, password}
  ▼
Nginx
  │ 生成 X-Trace-Id，写入真实来源 IP
  ▼
app-gateway
  │ 登录接口允许匿名
  │ Redis 检查来源 IP 是否超出登录额度
  │ Nacos 找 app-service
  ▼
app-service / AuthProxy
  │ 原样中转登录 JSON
  │ 使用配置 URL 找 platform-gateway
  ▼
platform-gateway
  │ 登录接口允许匿名
  │ Nacos 找 platform-service
  ▼
platform-service / AuthController
  │ MySQL 查账号并用 BCrypt 比较密码
  │ Redis 写入 lab:session:<token>
  ▼
token 原路返回浏览器
  │ 浏览器再请求 GET /api/auth/me
  ▼
页面显示当前用户名和权限
```

### 3.2 第一步：浏览器组成登录请求

文件：`frontend/app.js`

```javascript
const session = await request('/api/auth/login', 'POST', {
  username: $('username').value.trim(),
  password: $('password').value
});
token = session.token;
const profile = await request('/api/auth/me');
```

翻译：

1. 从页面输入框读取用户名和密码。
2. 向同一网站的 `/api/auth/login` 发送 POST 请求。
3. 登录成功后，把响应中的 token 放进 JavaScript 变量。
4. 再调用 `/api/auth/me`，确认 token 有效并取得用户名、权限。

这里的 `token` 只是页面内存变量：

```javascript
let token = null;
```

没有写入 `localStorage`、`sessionStorage` 或 Cookie。因此刷新页面后，浏览器这一侧会忘记 token。Redis 中尚未过期的服务端会话不会因为刷新页面自动删除。

`request(...)` 在后续请求中加入：

```javascript
if (token) headers.Authorization = 'Bearer ' + token;
```

密码提交完成后会清空输入框。请求信息区域不会展示密码、token 或完整认证响应。

### 3.3 第二步：Nginx 接收请求

文件：`infra/nginx/nginx.conf`

```nginx
location /api/ {
  proxy_pass http://app-gateway:8080;
  proxy_set_header X-Trace-Id $request_id;
  proxy_set_header X-Lab-Client-IP $remote_addr;
}
```

翻译：

- `/api/` 请求不由 Nginx 自己处理，而是转发到 `app-gateway:8080`。
- `$request_id` 是 Nginx 为这次外部请求生成的编号。
- `$remote_addr` 是 Nginx 实际看到的连接来源，用于登录限流。
- 外部调用者自己填写同名来源头时，Nginx 会覆盖它，不能简单换一个头绕过登录额度。

这一跳使用 Docker 内部 DNS 解析 `app-gateway`，不使用 Nacos。

### 3.4 第三步：应用层网关允许匿名登录

文件：`app-gateway/src/main/java/lab/SessionAuth.java`

```java
boolean publicPath = path.equals("/actuator/health")
    || (path.equals("/api/auth/login")
        && exchange.getRequest().getMethod() == HttpMethod.POST);

if (publicPath) return chain.filter(clean);
```

为什么登录接口必须放行？因为用户正准备换取 token，此时本来就不可能携带有效 token。

这里不是把整个 `/api/auth/**` 都设为公开，只精确放行 `POST /api/auth/login` 和所需健康检查。`GET /api/auth/me`、`POST /api/auth/logout` 仍然需要会话。

过滤器还会删除外部请求中这些身份头：

```java
headers.remove("X-User-Id");
headers.remove("X-User-Name");
headers.remove("X-User-Roles");
```

因此客户端不能写一个 `X-User-Roles: admin` 就把自己变成管理员。

### 3.5 第四步：登录限流先检查 Redis

文件：

- `app-gateway/src/main/java/lab/RateLimitFilter.java`
- `app-gateway/src/main/java/lab/RedisWindowLimiter.java`
- `app-gateway/src/main/resources/rate-window.lua`
- `app-gateway/src/main/resources/application.yml`

当前配置：

```yaml
lab.rate:
  login:
    limit: 10
    window-seconds: 30
```

含义是：同一个来源 IP 从窗口内第一次登录请求开始计时，30 秒最多 10 次。成功登录和失败登录都会占用额度。

过滤器先识别实际命中的路由：

```java
boolean login = route.getId().equals("app-login");
String identity = login ? clientAddress(exchange)
                        : exchange.getAttribute("lab.auth.username");
```

登录按来源 IP；工单查询按已经通过会话校验的用户名。计数 key 不直接放原始身份，而是：

```text
lab:rate:login:<身份的 SHA-256 摘要>
```

Lua 脚本把“读取计数、检查上限、增加计数、设置过期时间”作为一次 Redis 原子操作。达到上限返回 429 和 `Retry-After`；Redis 故障返回 503，不会为了可用性自动跳过限流。

### 3.6 第五步：应用网关通过 Nacos 找到 app-service

文件：`app-gateway/src/main/resources/application.yml`

```yaml
spring:
  application:
    name: app-gateway
  cloud:
    nacos:
      discovery:
        namespace: application

spring.cloud.gateway:
  routes:
    - id: app-login
      uri: lb://app-service
      predicates:
        - Path=/api/auth/login
        - Method=POST
```

这里的关键不是容器名，而是 `lb://app-service`：

1. `app-service` 启动后把自己注册到 Nacos 的 `application` 命名空间。
2. `app-gateway` 也连接 `application` 命名空间。
3. Gateway 根据服务名向 Nacos 获取健康实例。
4. 负载均衡器选择实例并转发。

这才是本项目中“通过 Nacos 找服务”的一跳。

### 3.7 第六步：app-service 只做中转

文件：`app-service/src/main/java/lab/AuthProxy.java`

```java
@PostMapping("/login")
ResponseEntity<String> login(@RequestBody String body) {
    return forward(HttpMethod.POST, "/api/auth/login", body);
}
```

它没有查账号，也没有自己生成 token。它把登录 JSON 交给统一的 `forward(...)` 方法，再调用中台网关。

文件：`app-service/src/main/java/lab/PlatformClient.java`

```java
@Bean
RestClient platformRestClient(@Value("${PLATFORM_GATEWAY_URL}") String url) {
    return RestClient.builder().baseUrl(url)
        .requestInterceptor((request, body, execution) -> {
            // 透传 Authorization 和 X-Trace-Id
            return execution.execute(request, body);
        }).build();
}
```

`PLATFORM_GATEWAY_URL` 来自 `compose.yaml`：

```yaml
PLATFORM_GATEWAY_URL: http://platform-gateway:8080
```

这一跳没有跨 Nacos 命名空间发现服务，而是使用部署配置给出的 URL。当前 Docker 环境把 `platform-gateway` 解析为中台网关容器地址，用它模拟真实项目里的内网 URL。

### 3.8 第七步：中台网关找到 platform-service

文件：`platform-gateway/src/main/resources/application.yml`

```yaml
- id: platform-service
  uri: lb://platform-service
  predicates:
    - Path=/api/tickets/**,/api/auth/**
```

中台网关和 `platform-service` 都在 Nacos 的 `platform` 命名空间。它使用 `lb://platform-service` 完成同层服务发现。

中台网关的 `SessionAuth` 同样精确放行登录请求。登录时还没有会话，因此这一层不能要求 token。

### 3.9 第八步：中台 AuthController 处理登录

文件：`platform-service/src/main/java/lab/AuthController.java`

```java
@PostMapping("/login")
ResponseEntity<?> login(@RequestBody Credentials credentials) {
    // 先检查用户名和密码格式
    var user = users.authenticate(credentials.username(), credentials.password());
    if (user == null) return reply(401, Map.of("error", "invalid_credentials"));
    String token = sessions.create(user);
    return reply(200, Map.of(
        "token", token,
        "tokenType", "Bearer",
        "expiresIn", SessionStore.TTL.toSeconds()));
}
```

代码分成四步：

1. 检查用户名、密码是否缺失，格式和长度是否合法。
2. 调用 `DemoUsers.authenticate(...)` 校验账号密码。
3. 校验成功后调用 `SessionStore.create(...)` 创建 Redis 会话。
4. 返回 token 类型和有效秒数，不返回密码、密码哈希或 Redis key。

密码限制为最多 72 个 UTF-8 字节，是当前 BCrypt 输入边界的一部分。

### 3.10 第九步：DemoUsers 在 MySQL 中校验密码

文件：`platform-service/src/main/java/lab/DemoUsers.java`

服务启动时执行：

```java
jdbc.execute("CREATE TABLE IF NOT EXISTS lab_users (...)");
```

所以 `lab_users` 不是由 `infra/mysql/001-init.sql` 创建的，而是 `DemoUsers` 在中台服务启动时幂等创建。

随后只补充不存在的 `demo` 和 `observer`。用简化写法表示：

```java
if (账号不存在) {
    INSERT username, encoder.encode(initialPassword), can_read
}
```

这里的 `initialPassword` 来自环境变量 `DEMO_PASSWORD`。写进 MySQL 的是：

```java
encoder.encode(initialPassword)
```

也就是 BCrypt 哈希，不是明文密码。

登录时：

```java
var rows = jdbc.query(
    "SELECT username, password_hash, can_read FROM lab_users WHERE username = ?",
    ...,
    username);
boolean matches = encoder.matches(password, hash);
```

参数中的 `?` 由 JDBC 绑定用户名，避免把用户名直接拼进 SQL。`encoder.matches` 使用用户输入的密码与数据库哈希比较。

当前权限转换规则：

- `demo.can_read = true` → 会话权限包含 `ticket:read`。
- `observer.can_read = false` → 可以登录、查询自己、退出，但不能查工单。

修改 `.env` 中的 `DEMO_PASSWORD` 不会自动改变已经存在的 MySQL 账号密码，因为启动逻辑只插入缺失账号，不更新已有记录。

### 3.11 第十步：SessionStore 在 Redis 中创建会话

文件：`platform-service/src/main/java/lab/SessionStore.java`

```java
static final Duration TTL = Duration.ofMinutes(30);

byte[] bytes = new byte[32];
random.nextBytes(bytes);
String token = HexFormat.of().formatHex(bytes);

redis.opsForValue().set(
    "lab:session:" + token,
    mapper.writeValueAsString(user),
    TTL);
```

逐步解释：

1. `SecureRandom` 生成 32 字节安全随机数。
2. 每个字节显示为两个十六进制字符，所以 token 是 64 位十六进制字符串。
3. Redis key 是 `lab:session:<token>`。
4. Redis value 是用户 JSON，例如用户名和权限列表。
5. key 固定 30 分钟过期；普通访问不会续期。

Redis 会话中不保存密码，也不保存密码哈希。

### 3.12 第十一步：响应原路返回浏览器

```text
platform-service 返回 token
→ platform-gateway
→ app-service
→ app-gateway
→ Nginx
→ 浏览器
```

浏览器验证 token 格式后先保存到内存，再调用 `/api/auth/me`。`/me` 会经过会话检查，成功后页面才显示“已登录”。

如果 token 已经创建，但 `/me` 请求失败，前端会尽力调用退出接口删除新会话；即使退出也失败，Redis TTL 最终会回收它。

### 3.13 登录前后数据变化

```text
登录前：
MySQL  lab_users 已有 demo 账号和 BCrypt 哈希
Redis  没有本次 token 对应的会话
浏览器 token = null

登录成功：
MySQL  用户记录不变化
Redis  新增 lab:session:<token>，TTL 约 1800 秒
浏览器 token = 服务端返回的 64 位随机字符串

刷新页面：
MySQL  不变化
Redis  会话仍在，直到退出或 TTL 到期
浏览器 token = null，因此页面显示未登录
```

### 3.14 登录自检

1. 为什么 `.env` 有演示密码，MySQL 又有 `password_hash`？
2. 为什么登录接口不能先要求 token？
3. `app-gateway → app-service` 和 `app-service → platform-gateway` 分别怎样找到目标？
4. Redis 会话中保存密码了吗？

参考答案：`.env` 是缺失账号的初始化输入，MySQL 保存 BCrypt 哈希；登录正是换取 token 的入口；前者使用 Nacos，后者使用配置 URL；Redis 会话只保存用户名和权限。

---

## 4. 账号、密码、token 和会话不要混为一谈

### 4.1 四个概念

| 概念 | 例子 | 谁创建 | 保存在哪里 | 用途 |
| --- | --- | --- | --- | --- |
| 账号 | `demo` | 中台初始化逻辑 | MySQL | 标识长期用户 |
| 密码哈希 | BCrypt 字符串 | `DemoUsers` | MySQL | 验证用户提供的密码，不可当原密码直接读取 |
| token | 64 位随机十六进制字符串 | `SessionStore` | 浏览器内存，同时嵌入 Redis key | 后续请求提交的会话凭证 |
| 服务端会话 | 用户名和权限 JSON | `SessionStore` | Redis | 判断 token 是否有效、代表谁、有什么权限 |

### 4.2 这里不是 JWT

JWT 通常把一些声明编码在 token 自身中。本项目的 token 是不透明随机字符串，仅靠字符串本身看不出用户名和权限。

服务端每次拿到 token 后，都需要查询：

```text
Redis key = lab:session:<token>
```

key 存在并能解析，才认为会话有效。退出时删除这个 key，可以立即让当前 token 失效。

### 4.3 权限为什么是“登录时快照”

登录时 `DemoUsers` 从 MySQL 读取 `can_read`，转换成权限列表，再把列表写入 Redis 会话。

因此管理员如果只修改 MySQL 的 `can_read`：

- 已有 Redis 会话仍保留旧权限。
- 用户退出并重新登录后，才会生成包含新权限的会话。

当前实验没有实现账号禁用通知、全端会话撤销或权限实时刷新。

### 4.4 点击退出以后发生了什么

退出不是简单地把页面隐藏起来，它有一条真实的服务端链路：

```text
浏览器 POST /api/auth/logout，携带 Authorization
→ 应用网关查 Redis，确认当前会话有效
→ app-service 的 AuthProxy 转发请求
→ PlatformClient 透传 Authorization
→ 中台网关再次确认会话
→ platform-service 的 SessionFilter 再次确认会话
→ AuthController.logout(...)
→ SessionStore.delete(token)
→ Redis 删除 lab:session:<token>
→ 成功响应原路返回
→ 浏览器清空本地 token、用户和工单结果
```

核心代码位于 `platform-service/src/main/java/lab/AuthController.java`：

```java
@PostMapping("/logout")
ResponseEntity<?> logout(@RequestHeader("Authorization") String authorization) {
    sessions.delete(SessionStore.token(authorization));
    return reply(200, Map.of("message", "logged_out"));
}
```

退出只删除当前 token 对应的一个会话，没有实现“强制该账号所有设备退出”。如果 Redis 故障导致退出请求失败，前端会保留当前本地登录状态并允许重试，不会把失败显示成已经退出。

---

## 5. 登录后查询工单，完整发生了什么

这一章只解决一个问题：带着 token 查询工单时，为什么经过三次会话/权限保护，最后才访问 MySQL？

### 5.1 查询总链路

```text
浏览器 GET /api/tickets/1
  │ Authorization: Bearer <token>
  ▼
Nginx：生成 TraceId，转发应用网关
  ▼
app-gateway / SessionAuth
  │ Redis 查会话，检查 ticket:read
  │ 将服务端确认的 username 放入当前请求属性
  ▼
app-gateway / RateLimitFilter
  │ Redis 按 username 检查 10 秒 20 次额度
  ▼
Nacos(application) → app-service
  ▼
app-service / TicketProxy
  │ PlatformClient 透传 Authorization、TraceId
  ▼
配置 URL → platform-gateway
  ▼
platform-gateway / SessionAuth
  │ Redis 再查会话、再查 ticket:read
  ▼
Nacos(platform) → platform-service
  ▼
platform-service / SessionFilter
  │ Redis 再查会话、再查 ticket:read
  ▼
platform-service / TicketController
  │ 参数化 SQL 查询 MySQL tickets
  ▼
JSON 工单原路返回浏览器
```

### 5.2 浏览器怎样携带 token

文件：`frontend/app.js`

```javascript
if (token) headers.Authorization = 'Bearer ' + token;
const ticket = await request('/api/tickets/' + encodeURIComponent(id));
```

浏览器先检查工单 ID 是合法正整数，再发出请求。前端校验是为了更快提示用户，不是安全边界；服务端仍必须自己校验和保护接口。

### 5.3 应用网关怎样校验会话

文件：`app-gateway/src/main/java/lab/SessionAuth.java`

第一步，Authorization 必须只有一个，并符合：

```text
Bearer + 空格 + 64 位十六进制 token
```

第二步，从 Redis 读取：

```java
redis.opsForValue().get("lab:session:" + token)
```

第三步，解析用户 JSON：

- 没有 key：401，可能缺失、过期或已经退出。
- Redis 异常或内容损坏：503，不把存储故障误报成未登录。
- `/api/auth/me` 或 `/api/auth/logout`：有效会话即可。
- 其他业务路径：还要包含 `ticket:read`，否则 403。

第四步，把 Redis 中可信的用户名放入当前请求内部属性：

```java
clean.getAttributes().put("lab.auth.username", username);
```

这个属性只在当前网关处理过程中使用，不接受浏览器传来的同名身份声明。

### 5.4 工单限流怎样取得用户身份

文件：`app-gateway/src/main/java/lab/RateLimitFilter.java`

```java
String identity = exchange.getAttribute("lab.auth.username");
String key = key("tickets", identity);
```

工单额度不按 token 计算，而按已经验证的用户名计算。这样同一个用户申请两个 token，也仍然共享同一额度。

当前规则：同一用户名 10 秒最多 20 次工单请求。匿名请求先返回 401、无权限请求先返回 403，它们不会消耗工单查询额度。`/me`、`/logout` 和健康检查不消耗工单额度。

### 5.5 app-service 怎样把请求送到中台

文件：`app-service/src/main/java/lab/TicketProxy.java`

```java
@GetMapping("/api/tickets/{id}")
ResponseEntity<String> get(@PathVariable long id) {
    var upstream = client.get()
        .uri("/api/tickets/{id}", id)
        .retrieve()
        .toEntity(String.class);
    return reply(upstream.getStatusCode(), upstream.getHeaders(), upstream.getBody());
}
```

这里的 `client` 就是 `PlatformClient` 创建的 `RestClient`。拦截器会把当前请求的 Authorization 和 TraceId 加到发往中台网关的新 HTTP 请求中。

`app-service` 处理了两类网络错误：

- 连接失败：返回 502 `platform_unavailable`。
- 等待响应超时：返回 504 `platform_timeout`。

它不会为了“看起来成功”而伪造中台数据。

### 5.6 中台网关为什么还要再校验

中台网关入口有自己的 `SessionAuth.java`，逻辑与应用网关的小型策略保持一致。

原因不是“前面的代码没用”，而是中台网关代表新的信任边界：

- 不能假设所有调用者永远都只从应用网关进入。
- 将来可能出现另一个应用层或内部调用方。
- 即使应用层某个服务配置错误，中台入口仍有保护。

当前验收还会在容器内部直接访问中台网关，确认匿名请求得到 401、无权限用户得到 403。

### 5.7 platform-service 为什么最后还检查一次

文件：`platform-service/src/main/java/lab/SessionFilter.java`

它在 Controller 前执行：

```java
String token = SessionStore.token(request.getHeader("Authorization"));
user = sessions.find(token);

if (!ownAccount && !user.permissions().contains("ticket:read")) {
    reject(response, 403, "forbidden");
    return;
}

request.setAttribute("sessionUser", user);
chain.doFilter(request, response);
```

中台服务是最终执行业务和读数据库的位置。即使有人设法绕过中台网关、直接访问服务容器，服务本身仍不会匿名执行查询。

三层保护的职责可以概括为：

| 层 | 主要意义 |
| --- | --- |
| 应用层网关 | 对外入口尽早拒绝，避免无效请求进入后续服务 |
| 中台层网关 | 保护中台统一入口，不完全信任上游应用层 |
| 中台层服务 | 最终业务资源自我保护，防止绕过网关直接访问 |

这是当前实验的防御纵深设计，不代表所有公司都必须恰好查询同一个 Redis 三次。生产环境可能使用网关签名、内部身份、服务网格或独立鉴权服务，但不能在没有其他可信机制时简单删除后两层。

### 5.8 TicketController 怎样查询 MySQL

文件：`platform-service/src/main/java/lab/TicketController.java`

```java
@GetMapping("/api/tickets/{id}")
ResponseEntity<?> get(@PathVariable long id) {
    var rows = jdbc.query(
        "SELECT id, title, status FROM tickets WHERE id = ?",
        (rs, n) -> new Ticket(
            rs.getLong("id"),
            rs.getString("title"),
            rs.getString("status")),
        id);
    // ...
}
```

解释：

1. `/api/tickets/1` 中的 `1` 被转换成 Java `long id`。
2. SQL 使用 `?` 占位符，不把 ID 拼接到 SQL 字符串里。
3. 每一行结果转换成 `Ticket(id, title, status)`。
4. 没查到返回 404 `ticket_not_found`。
5. 查到返回 200 和工单 JSON。
6. 数据库访问异常返回 503 `database_unavailable`。

`tickets` 表和初始工单由 `infra/mysql/001-init.sql` 在空 MySQL 数据卷第一次启动时创建。它与 `lab_users` 的创建位置不同。

### 5.9 响应怎样返回

```text
TicketController 的 JSON
→ platform-gateway 添加学习响应头
→ app-service 保留状态、正文和中台学习响应头
→ app-gateway 添加应用网关学习响应头
→ Nginx 统一返回入口生成的 X-Trace-Id
→ frontend/app.js 更新页面
```

`X-Lab-*` 只是教学标记，用来确认经过了哪些层；真正用于日志关联的是 `X-Trace-Id`。

### 5.10 查询自检

1. 为什么工单限流使用用户名，而不是 token？
2. 为什么 `app-service` 必须透传 Authorization？
3. 应用层服务调用中台网关为什么不使用 Nacos？
4. 为什么已经有网关，中台服务还保留 `SessionFilter`？

参考答案：同一用户的多个 token 应共享额度；后续中台必须验证会话；该跳使用部署配置 URL；服务需要防止绕过网关直接访问。

---

## 6. 登录、鉴权和权限分别是什么

这三个词经常被混在一起：

- 登录（authentication 的入口）：用用户名和密码证明“你可能是谁”，换取 token。
- 会话鉴别（authentication）：用 token 和 Redis 会话确认“这个请求现在代表谁”。
- 权限判断（authorization）：确认这个已经登录的用户“能不能查工单”。

当前项目的判断顺序：

```text
Authorization 格式是否合法？
→ Redis 会话是否存在？
→ 会话 JSON 是否有效？
→ 当前接口只要求登录，还是要求 ticket:read？
→ 通过后才进入下一层
```

`demo` 和 `observer` 都能登录，因此两者密码正确时都是认证成功。但是只有 `demo` 的会话包含 `ticket:read`，所以 `observer` 查工单得到 403。

---

## 7. 常见 HTTP 状态码是在哪一层产生的

| 状态码 | 当前项目含义 | 常见产生位置 | 请求是否继续向后 |
| --- | --- | --- | --- |
| 200 | 登录、查询、个人信息或退出成功 | Controller | 已执行完整目标链路 |
| 400 | 登录字段或工单 ID 格式不合法 | `AuthController`、`app-service` 参数绑定 | 在当前服务停止 |
| 401 | 密码错误，或 token 缺失、无效、过期、已退出 | `AuthController`、各鉴权过滤器 | 在产生 401 的层停止 |
| 403 | 会话有效但没有 `ticket:read` | 两个网关或中台服务 | 在产生 403 的层停止 |
| 404 | 已授权但路由不存在，或工单不存在 | 网关或 `TicketController` | 取决于 404 的位置 |
| 429 | 登录或工单额度用完 | 应用层网关 `RateLimitFilter` | 不进入应用服务 |
| 502 | 应用服务无法连接中台网关 | `TicketProxy` / `AuthProxy` | 停在应用服务 |
| 503 | Redis、认证存储、数据库或可用实例异常 | 网关或服务 | 停在发现故障的位置 |
| 504 | 应用服务等待中台网关超时 | `TicketProxy` / `AuthProxy` | 停在应用服务 |

同一个 URL 不一定永远返回同一种错误。例如匿名访问一个不存在的 `/api/missing`，可能先在应用网关得到 401；有效用户通过鉴权后，才会看到路由 404。这体现了“默认先鉴权”的策略。

---

## 8. 网关里的代码按什么顺序工作

以应用网关为主，可以按下面的顺序理解：

```text
RequestLog
  │ 建立/校验 TraceId，准备完成日志
  ▼
SessionAuth
  │ 删除伪造身份头，公开路径放行，其余请求查 Redis 会话和权限
  ▼
Gateway 路由匹配
  │ app-login / app-tickets / app-auth
  ▼
RateLimitFilter
  │ 只对登录和工单路由扣 Redis 额度
  ▼
负载均衡 + Nacos 实例
  ▼
app-service
```

重要代码位置：

| 要找什么 | 文件 |
| --- | --- |
| 路由路径和 `lb://app-service` | `app-gateway/src/main/resources/application.yml` |
| 会话和权限检查 | `app-gateway/src/main/java/lab/SessionAuth.java` |
| 登录与工单限流 | `app-gateway/src/main/java/lab/RateLimitFilter.java` |
| Redis Lua 调用 | `app-gateway/src/main/java/lab/RedisWindowLimiter.java` |
| 固定窗口算法 | `app-gateway/src/main/resources/rate-window.lua` |
| TraceId 和完成日志 | `app-gateway/src/main/java/lab/RequestLog.java` |

中台网关没有重复扣限流额度，但有自己的 `SessionAuth`、`RequestLog` 和 `application.yml` 路由。

---

## 9. TraceId 怎样从入口传到数据库查询日志

### 9.1 TraceId 解决什么问题

同时有多个用户请求 `/api/tickets/1` 时，只看 URL 无法判断哪些日志属于同一次请求。TraceId 给每个外部请求一个编号，用这个编号串联实际经过的层。

它不是：登录 token、用户身份、权限凭证、数据库主键，也不是完整的分布式 tracing span。

### 9.2 传播链路

```text
Nginx $request_id
  │ 请求头 X-Trace-Id
  ▼
app-gateway RequestLog
  │ 请求头 X-Trace-Id
  ▼
app-service RequestTraceFilter
  │ MDC traceId
  ▼
PlatformClient 拦截器
  │ 请求头 X-Trace-Id
  ▼
platform-gateway RequestLog
  │ 请求头 X-Trace-Id
  ▼
platform-service RequestTraceFilter
  │ MDC traceId
  ▼
TicketController 的 MySQL 查询业务日志
```

### 9.3 Nginx 是外部请求的编号入口

文件：`infra/nginx/nginx.conf`

```nginx
proxy_set_header X-Trace-Id $request_id;
proxy_hide_header X-Trace-Id;
add_header X-Trace-Id $request_id always;
```

- 发给应用网关时，使用 Nginx 的 `$request_id`。
- 隐藏上游同名响应头，避免浏览器收到多个不同值。
- 无论成功还是错误，入口都返回这个编号。
- 外部用户提交的 X-Trace-Id 会被覆盖，避免人为复用编号干扰日志判断。

### 9.4 Gateway 为什么不使用线程 MDC

文件：两个网关的 `RequestLog.java`

Gateway 的响应式回调可能切换线程。把 TraceId 放在线程本地 MDC，可能出现同一请求换线程后丢失，或者线程复用造成串号。

所以网关把 TraceId 保存在当前过滤器的局部变量、当前请求头和响应提交前的回调闭包。网关接受合法的 32 位小写十六进制编号；容器内直接调用时若没有合法编号，会自己补发一个，保证日志仍可关联。

### 9.5 普通 MVC 服务为什么使用 MDC

文件：

- `app-service/src/main/java/lab/RequestTraceFilter.java`
- `platform-service/src/main/java/lab/RequestTraceFilter.java`

```java
String previous = MDC.get("traceId");
MDC.put("traceId", traceId);
try {
    chain.doFilter(request, response);
} finally {
    if (previous == null) MDC.remove("traceId");
    else MDC.put("traceId", previous);
}
```

同步 Servlet 请求通常由当前线程执行，Spring 日志格式从 MDC 读取 `traceId`。`finally` 中清理非常重要，因为 Tomcat 会复用线程；不清理可能导致下一次请求错误继承上一次编号。

### 9.6 app-service 怎样把编号送到下一次 HTTP 请求

浏览器到 app-service 是一次 HTTP 请求；app-service 调用中台网关是另一次 HTTP 请求。线程 MDC 不会自动跨网络。

`PlatformClient` 明确读取 MDC，再设置新的 HTTP 请求头 `X-Trace-Id`。这一步才把编号带到中台链路。

### 9.7 怎样用 TraceId 查日志

浏览器查询工单后，复制页面底部 TraceId，在项目目录执行：

```powershell
pwsh.exe -NoProfile -File .\scripts\show-trace.ps1 -TraceId 这里替换为32位编号
```

脚本查询最近 30 分钟内的 `nginx`、`app-gateway`、`app-service`、`platform-gateway` 和 `platform-service`。

正常工单查询应看到五层完成日志；如果请求在应用网关因 401、403 或 429 被拒绝，只应看到 Nginx 和应用网关日志。没有到达的服务不会凭空产生这次请求的日志。

### 9.8 耗时为什么不能直接相加

- Nginx 记录 `durationSec`，单位秒。
- Java 记录 `durationMs`，单位毫秒。
- 外层耗时本来就包含等待内层的时间。
- 网关和 Servlet 的计时结束边界也不完全相同。

因此五层耗时不能相加当成总耗时。排障时通常从外到内比较，找出哪一段增加明显。

更详细的操作见 [《TraceId学习与操作》](docs/TraceId学习与操作.md)。

### 9.9 TraceId 自检

1. TraceId 和 token 的用途有什么不同？
2. 为什么 app-service 的 RestClient 必须主动设置 X-Trace-Id？
3. 为什么 Gateway 不使用普通线程 MDC？
4. 一次 403 请求只出现两层日志，是日志丢了吗？

参考答案：前者关联日志、后者证明会话；TraceId 不会自动跨 HTTP；响应式处理可能切换线程；请求在应用网关已停止，后续没有日志是正确现象。

---

## 10. Redis 在整个项目里做了什么

### 10.1 Redis 不保存哪些东西

当前 Redis 不保存用户密码或密码哈希、工单正文、TraceId 日志或 Nacos 服务实例。

### 10.2 Redis 的两类业务 key

| 用途 | key 形状 | value | 过期方式 |
| --- | --- | --- | --- |
| 登录会话 | `lab:session:<64位token>` | 用户名和权限 JSON | 固定 30 分钟，不因访问续期 |
| 登录限流 | `lab:rate:login:<身份摘要>` | 当前固定窗口计数 | 30 秒窗口 |
| 工单限流 | `lab:rate:tickets:<身份摘要>` | 当前固定窗口计数 | 10 秒窗口 |

限流身份使用 SHA-256 摘要可以减少 key 中直接暴露 IP 或用户名，但摘要不等于加密或彻底匿名化。

### 10.3 固定窗口限流怎么工作

```text
key 不存在或已过期
→ 设置计数为 1，并设置窗口 TTL
→ 放行

key 存在且计数小于 limit
→ 加 1
→ 放行

key 存在且计数已经达到 limit
→ 不再增加，不延长 TTL
→ 返回剩余 TTL，网关转换成 Retry-After
```

Lua 脚本在 Redis 内一次执行，避免多个网关实例同时“先读后写”导致额度计算不一致。它是固定窗口，不是滑动窗口或令牌桶，窗口交界处仍可能出现短时突发。

### 10.4 Redis 故障为什么选择拒绝

Redis 不可用时，网关无法确认 token、权限或额度。当前策略是 fail closed：返回 503，而不是跳过校验继续访问。否则 Redis 一停，匿名用户反而可能得到业务权限。

### 10.5 Redis 自己如何运行

`compose.yaml` 定义：

- 容器内部端口 `6379`。
- Windows 工具访问 `127.0.0.1:16379`。
- 容器之间访问 `redis:6379`。
- 密码来自本机 `.env` 的 `REDIS_PASSWORD`。
- 使用 AOF，`appendfsync everysec`。
- 数据卷为 `redis-data`。
- `maxmemory-policy noeviction`，内存不足时不静默淘汰会话 key。

Redis 不注册到 Nacos，地址来自部署配置。更详细的连接和验收见 [《Redis学习与操作》](docs/Redis学习与操作.md)，限流细节见 [《限流学习与操作》](docs/限流学习与操作.md)。

---

## 11. Docker 地址、端口和 Nacos 服务发现

### 11.1 `18080:80` 到底是什么意思

```yaml
ports: ['127.0.0.1:18080:80']
```

格式是“宿主机地址 : 宿主机端口 : 容器端口”。浏览器访问 Windows 的 `127.0.0.1:18080`，Docker 转发到 Nginx 容器的 80。Nginx 自己只知道它监听 80，并不知道宿主机选择了 18080。

### 11.2 Docker 服务名解析

所有服务默认加入同一个 Compose 网络，因此容器可以使用服务名访问彼此：

```text
nginx → app-gateway:8080
Java 服务 → nacos:8848
中台服务 → mysql:3306
网关/中台服务 → redis:6379
app-service → platform-gateway:8080
```

Docker 把这些名称解析成当前容器 IP。容器重建、IP 改变后，调用方仍然使用稳定的服务名。

### 11.3 Nacos 服务发现

项目只运行一个 Nacos 容器，但创建两个命名空间：

| Nacos 命名空间 | 注册服务 |
| --- | --- |
| `application` | `app-gateway`、`app-service` |
| `platform` | `platform-gateway`、`platform-service` |

命名空间限制服务注册和发现的范围，不等于两个 Docker 网络。

当前每一跳的查找机制：

| 调用 | 怎样找到目标 | 是否使用 Nacos |
| --- | --- | --- |
| 浏览器 → Nginx | Docker 宿主机端口映射 | 否 |
| Nginx → app-gateway | Docker 服务名 `app-gateway` | 否 |
| app-gateway → app-service | `lb://app-service` + Nacos `application` | 是 |
| app-service → platform-gateway | `PLATFORM_GATEWAY_URL` + Docker DNS | 否 |
| platform-gateway → platform-service | `lb://platform-service` + Nacos `platform` | 是 |
| platform-service → MySQL | Docker 服务名 `mysql` | 否 |
| 网关/服务 → Redis | Docker 服务名 `redis` | 否 |

一句话记忆：Docker DNS 解决“这个容器名当前是哪一个 IP”；Nacos 解决“这个微服务名目前有哪些健康实例可供网关选择”。

---

## 12. 从浏览器实际学习一次

### 12.1 启动项目

Docker Desktop 保持运行，在项目目录使用 PowerShell 7：

```powershell
pwsh.exe -NoProfile -File .\scripts\start.ps1 -SkipBuild
```

改过 Java 代码后不要使用 `-SkipBuild`，需要重新测试、构建镜像：

```powershell
pwsh.exe -NoProfile -File .\scripts\start.ps1
```

### 12.2 实验一：只观察登录

1. 打开 `http://127.0.0.1:18080/`。
2. 用户名输入 `demo`。
3. 密码从本地 `.env` 的 `DEMO_PASSWORD` 读取，只输入等号后面的值。
4. 点击登录。
5. 暂时不要查询工单，只观察页面显示的用户名和 `ticket:read` 权限。
6. 回到第 3 章，口头复述 token 是在哪个类生成、会话写到了哪里。

不要把真实密码或 token 复制进聊天、截图或项目文档。

### 12.3 实验二：比较 demo 和 observer

1. 使用 `demo` 登录并查询工单 1，应成功。
2. 退出。
3. 使用 `observer` 和相同初始密码登录。
4. 查询工单 1，应得到 403 提示，但登录状态仍保留。
5. 思考：这是登录失败，还是权限不足？

答案：`observer` 已成功登录，会话有效，只是没有 `ticket:read`。

### 12.4 实验三：观察退出

1. 登录后点击退出。
2. 页面清空 token、用户和工单结果。
3. 服务端 `POST /api/auth/logout` 删除 Redis 中当前 `lab:session:<token>`。
4. 再使用旧 token 时应返回 401。

刷新页面与退出不同：刷新只丢失浏览器变量，不会主动删除 Redis 会话。

### 12.5 实验四：用 TraceId 找五层日志

1. 使用 `demo` 查询工单 1。
2. 复制页面底部 TraceId。
3. 执行：

```powershell
pwsh.exe -NoProfile -File .\scripts\show-trace.ps1 -TraceId 这里替换为编号
```

4. 按 Nginx、应用网关、应用服务、中台网关、中台服务的顺序读完成日志。
5. 找到中台服务“查询 MySQL”的业务日志，确认它使用相同 TraceId。

### 12.6 实验五：观察 401、403、404

- 未登录查询工单：401，通常只到应用网关。
- observer 查询工单：403，通常只到应用网关。
- demo 查询不存在的工单 `999999`：404，完整到达中台服务。

比较三个 TraceId 的日志层数，比背诵状态码更容易理解“请求在哪里停止”。

### 12.7 已有自动验收入口

| 目的 | 脚本 |
| --- | --- |
| 基础完整链路 | `scripts/verify.ps1` |
| 登录、会话、权限 | `scripts/verify-auth.ps1` |
| TraceId 和日志层数 | `scripts/verify-trace.ps1` |
| Redis 配置、TTL 和连接 | `scripts/verify-redis.ps1` |
| 登录和工单限流 | `scripts/verify-rate.ps1` |
| 浏览器静态页面入口 | `scripts/verify-ui.ps1` |
| 按编号查询日志 | `scripts/show-trace.ps1` |

带 `-IncludeFailure` 的专项脚本可能短暂停止本项目组件并自动恢复。第一次学习时先做普通浏览器实验，不必立刻运行故障实验。

---

## 13. 我想知道某件事，应该先看哪个文件

| 问题 | 第一入口 | 接着看 |
| --- | --- | --- |
| 登录按钮点击后做什么 | [`frontend/app.js`](frontend/app.js) | [`AuthProxy.java`](app-service/src/main/java/lab/AuthProxy.java)、[`AuthController.java`](platform-service/src/main/java/lab/AuthController.java) |
| 页面结构在哪里 | [`frontend/index.html`](frontend/index.html) | [`frontend/styles.css`](frontend/styles.css) |
| Nginx 怎样转发 `/api` | [`infra/nginx/nginx.conf`](infra/nginx/nginx.conf) | [`compose.yaml`](compose.yaml) 的 nginx 段 |
| 应用网关路由在哪里 | [`app-gateway/application.yml`](app-gateway/src/main/resources/application.yml) | Nacos `application` 命名空间 |
| 登录为什么允许匿名 | [`app-gateway/SessionAuth.java`](app-gateway/src/main/java/lab/SessionAuth.java) | [`platform-gateway/SessionAuth.java`](platform-gateway/src/main/java/lab/SessionAuth.java) |
| 登录接口最终在哪里 | [`AuthController.java`](platform-service/src/main/java/lab/AuthController.java) | [`DemoUsers.java`](platform-service/src/main/java/lab/DemoUsers.java)、[`SessionStore.java`](platform-service/src/main/java/lab/SessionStore.java) |
| 用户表在哪里创建 | [`DemoUsers.java`](platform-service/src/main/java/lab/DemoUsers.java) | MySQL `lab_users` |
| 初始密码从哪里来 | `.env` 的 `DEMO_PASSWORD` | [`compose.yaml`](compose.yaml)、[`DemoUsers.java`](platform-service/src/main/java/lab/DemoUsers.java) |
| 密码怎样校验 | [`DemoUsers.authenticate(...)`](platform-service/src/main/java/lab/DemoUsers.java) | `BCryptPasswordEncoder.matches(...)` |
| token 怎样生成 | [`SessionStore.java`](platform-service/src/main/java/lab/SessionStore.java) | Redis `lab:session:<token>` |
| token 怎样传给中台 | [`PlatformClient.java`](app-service/src/main/java/lab/PlatformClient.java) | Authorization 请求头 |
| 两层网关怎样鉴权 | [`app-gateway/SessionAuth`](app-gateway/src/main/java/lab/SessionAuth.java)、[`platform-gateway/SessionAuth`](platform-gateway/src/main/java/lab/SessionAuth.java) | Redis 会话 |
| 中台服务怎样防绕过 | [`SessionFilter.java`](platform-service/src/main/java/lab/SessionFilter.java) | `SessionStore.find(...)` |
| 工单接口在哪里 | [`TicketController.java`](platform-service/src/main/java/lab/TicketController.java) | MySQL `tickets` |
| 应用层为什么只是中转 | [`TicketProxy.java`](app-service/src/main/java/lab/TicketProxy.java)、[`AuthProxy.java`](app-service/src/main/java/lab/AuthProxy.java) | [`PlatformClient.java`](app-service/src/main/java/lab/PlatformClient.java) |
| 工单 SQL 在哪里 | [`TicketController.java`](platform-service/src/main/java/lab/TicketController.java) | [`infra/mysql/001-init.sql`](infra/mysql/001-init.sql) |
| 登录和工单限流 | [`RateLimitFilter.java`](app-gateway/src/main/java/lab/RateLimitFilter.java) | [`RedisWindowLimiter.java`](app-gateway/src/main/java/lab/RedisWindowLimiter.java)、[`rate-window.lua`](app-gateway/src/main/resources/rate-window.lua) |
| TraceId 在哪里产生 | [`infra/nginx/nginx.conf`](infra/nginx/nginx.conf) | 两个网关的 [`RequestLog.java`](app-gateway/src/main/java/lab/RequestLog.java) |
| Java 日志为什么带 TraceId | [`app-service/RequestTraceFilter`](app-service/src/main/java/lab/RequestTraceFilter.java)、[`platform-service/RequestTraceFilter`](platform-service/src/main/java/lab/RequestTraceFilter.java) | 两个服务的 `application.yml` 日志格式 |
| 服务怎样注册 Nacos | 四个模块的 `application.yml` | [`scripts/init-nacos.ps1`](scripts/init-nacos.ps1) |
| 容器、端口、环境变量 | [`compose.yaml`](compose.yaml) | [`README.md`](README.md) |
| 哪些行为已经验收 | [`搭建日志.md`](搭建日志.md) | 各 `scripts/verify-*.ps1` |

---

## 14. 按一次请求排查问题

遇到“查询失败”，不要立刻从所有服务日志中乱找。按顺序检查：

```text
1. 浏览器得到了哪个 HTTP 状态？
2. 响应有没有 X-Trace-Id？
3. 页面展示的是登录、权限、限流、网络还是业务错误？
4. 用 show-trace.ps1 查询这个编号。
5. 最后一条出现在哪个服务？
6. 根据最后一层决定下一份代码：
   app-gateway       → SessionAuth / RateLimitFilter / 路由
   app-service       → TicketProxy / AuthProxy / PlatformClient
   platform-gateway  → SessionAuth / 路由 / Nacos 实例
   platform-service  → SessionFilter / AuthController / TicketController
```

例子：

- 只有 Nginx 和 app-gateway，状态 401：优先查 token、Redis 会话和 `SessionAuth`。
- 只有 Nginx 和 app-gateway，状态 429：优先查 `RateLimitFilter`、Redis 限流 key 和 `Retry-After`。
- 到 app-service 后返回 504：查 `PLATFORM_GATEWAY_URL`、中台网关健康和连接超时。
- 五层都有日志且状态 404：链路正常，查 `TicketController` 和 MySQL 是否有该工单。
- 五层都有日志且状态 503：结合中台服务业务日志区分 Redis 与 MySQL 故障。

---

## 15. 怎样把这套项目讲成面试表达

不要说“我搭了一个很复杂的微服务平台”，也不要只背组件名称。可以按真实链路表达：

```text
背景：为了理解真实项目中的双层网关和应用层到中台层调用，我搭建了一个可运行的最小工单系统。

链路：浏览器请求先到 Nginx，再经过应用网关、应用服务、中台网关和中台服务，最终查询 MySQL。

身份：账号和 BCrypt 密码哈希保存在 MySQL；登录后生成随机 token，Redis 保存 30 分钟服务端会话。

权限：应用网关尽早拒绝无效请求，中台网关保护中台入口，中台服务做最终资源保护；无权限用户返回 403。

稳定性：Redis 同时承担会话和固定窗口限流。Redis 故障时鉴权与限流选择拒绝，不静默放行。

排障：Nginx 生成 TraceId，两个网关和两个服务继续传递，用同一个编号可以定位请求停在哪一层。

边界：这是本地学习实现，已提供单机 Kubernetes 部署，尚未实现 Kafka、SSO、完整 RBAC 或 OpenTelemetry 后端。
```

如果面试官继续问，再挑一个具体难点展开，例如：

- 为什么不能信任浏览器传来的角色头。
- 为什么响应式网关不直接使用线程 MDC。
- 为什么同一用户多个 token 应共享工单限流额度。
- 为什么账号在 MySQL，而会话在 Redis。

---

## 16. 推荐的逐章学习节奏

不要以“读完全文”为目标，按下面的节奏更有效：

| 次序 | 本次只学什么 | 学完的判断标准 |
| --- | --- | --- |
| 1 | 系统全景 | 能说出八个容器各自职责 |
| 2 | 登录到 MySQL 校验 | 能从 `app.js` 追到 `DemoUsers` |
| 3 | Redis 会话创建 | 能区分密码、token、会话和权限快照 |
| 4 | 带 token 查询工单 | 能从应用网关追到 `TicketController` |
| 5 | 三层鉴权 | 能解释每层保护的边界 |
| 6 | TraceId | 能拿一个编号找到实际经过的日志 |
| 7 | 限流 | 能解释登录按 IP、查询按用户名 |
| 8 | Nacos 与 Docker | 能判断每一跳用的是哪种寻址方式 |
| 9 | 故障链路 | 能根据状态码和最后日志层定位范围 |
| 10 | 面试表达 | 能用两分钟讲清背景、链路、实现和边界 |

下一次学习时，可以直接提出：

```text
请阅读 E:\工作\面经\NovaCloud\学习文档.md，带我学习第 3 章登录链路。
一次只讲一个小节，结合真实代码提问，不要一次讲完一整章。
```

---

## 附录 A：专题文档

- [浏览器工作台学习与操作](docs/浏览器工作台学习与操作.md)
- [登录鉴权学习与操作](docs/登录鉴权学习与操作.md)
- [Redis学习与操作](docs/Redis学习与操作.md)
- [限流学习与操作](docs/限流学习与操作.md)
- [TraceId学习与操作](docs/TraceId学习与操作.md)
- [第一阶段实施方案](docs/第一阶段实施方案.md)

## 附录 B：事实来源的优先级

学习时遇到冲突，按下面的顺序判断：

1. 当前源码和配置：回答“现在代码写的是什么”。
2. 当前自动测试和验收脚本：回答“代码准备验证什么”。
3. 《搭建日志》中的实际结果：回答“当时真实运行发生了什么”。
4. 实施方案：回答“当时打算怎样做”，不能单独证明已经完成。
5. 本学习文档：帮助理解以上事实，不代替源码和验收证据。

旧日志可能因容器重建或轮转消失；历史验收结果也不自动等于今天的运行状态。需要确认当前状态时，应重新运行相应验收。
