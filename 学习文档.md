# NovaCloud 微服务架构学习文档

> 本文用于理解项目中的基础设施和请求调用链。代码搭建过程、故障和验证结果记录在《搭建日志.md》中。

## 学习目标

通过一个可以实际运行的最小项目，理解真实工作项目中的这条链路：

```text
前端请求
  → Nginx
  → 应用层 Gateway
  → 应用层服务
  → 中台层 Gateway
  → 中台层服务
  → MySQL
```

当前示例接口：

```text
GET http://127.0.0.1:18080/api/tickets/1
```

正常结果：

```json
{"id":1,"title":"Demo after-sales ticket","status":"OPEN"}
```

## 已确认的真实项目背景

- 应用层 Gateway 和应用层服务位于同一个 Nacos 命名空间。
- 中台层 Gateway 和中台层服务位于另一个 Nacos 命名空间。
- 应用层服务在实际业务中主要承担中转，再调用中台层处理业务。
- 根据现有回忆，应用层服务通过 Kubernetes 部署配置提供的内网 URL 调用中台 Gateway，不通过 Nacos 跨命名空间发现。
- 实验使用 Docker 内部地址 `http://platform-gateway:8080` 模拟这个内网 URL。
- 真实项目内网地址的具体名称和配置位置尚未查证，因此这里只记录为待确认信息。

## 当前实验如何运行

所有组件都运行在 Docker 容器中，没有分别安装到 Windows：

```text
Windows 电脑
└── Docker
    ├── Nginx
    ├── app-gateway
    ├── app-service
    ├── platform-gateway
    ├── platform-service
    ├── Nacos
    ├── MySQL
    └── Redis（中台服务已接入，登录会话业务待实现）
```

项目当前使用一套 Nacos，其中创建了两个命名空间：

| 命名空间 | 注册的服务 |
| --- | --- |
| `application` | `app-gateway`、`app-service` |
| `platform` | `platform-gateway`、`platform-service` |

命名空间限定服务注册和发现的范围。它不是容器，也不是网络隔离区。

## 第一课：请求如何从 Nginx 到应用层网关

浏览器访问：

```text
http://127.0.0.1:18080/api/tickets/1
```

第一段实际路径是：

```text
浏览器
  → Windows 的 18080 端口
  → Nginx 容器的 80 端口
  → app-gateway 容器的 8080 端口
```

`compose.yaml` 将 Windows 的 `127.0.0.1:18080` 映射到 Nginx 容器的 `80` 端口。Nginx 随后根据 `infra/nginx/nginx.conf` 转发请求：

```nginx
location /api/ {
    proxy_pass http://app-gateway:8080;
}
```

含义是：路径以 `/api/` 开头的请求交给 `app-gateway:8080`。

这一跳不使用 Nacos。Nginx 通过 Docker 内部网络解析 `app-gateway` 这个容器服务名。Nginx 不需要知道应用层服务、更不需要知道中台层服务或 MySQL 在哪里。

实验响应中包含：

```text
X-Lab-Nginx: visited
```

这个响应头是学习用标记，说明请求经过了 Nginx；它不是 TraceId，也不能代替正式的链路追踪。

### 第一课自检

1. 浏览器为什么访问 `18080`，而 Nginx 容器监听的是 `80`？
2. Nginx 如何知道 `app-gateway` 的容器地址？
3. 这一跳是否使用了 Nacos？

参考答案：Docker 做端口映射；Docker 内部网络解析服务名；这一跳不使用 Nacos。

## 后续学习顺序

每次只学习一个环节，并结合真实配置、请求和日志验证：

1. 应用层 Gateway 如何通过 Nacos 找到应用层服务。
2. 应用层服务如何读取中台 Gateway URL 并完成中转。
3. 两个 Nacos 命名空间如何限制服务发现范围。
4. 中台层 Gateway 如何找到中台层服务。
5. 中台层服务如何查询 MySQL，结果如何原路返回。
6. TraceId 与请求日志已实现，操作见 [TraceId学习与操作](docs/TraceId学习与操作.md)；学习进度由学习任务另行确认。
7. Redis 部署与 Java 连接见 [Redis学习与操作](docs/Redis学习与操作.md)，作为登录会话和限流的存储基础。
8. 登录、退出、身份校验及接口权限见 [登录鉴权学习与操作](docs/登录鉴权学习与操作.md)；Gateway 限流待实现。
9. 加入 Kafka，理解同步请求之外的消息通信。
10. 把同一套服务迁移到本机 Kubernetes。

## 学习与搭建的边界

- 本文解释已经实现或明确标注为计划的内容。
- 《搭建日志.md》记录实际执行结果，不能把计划写成已经验证的事实。
- 当前第一阶段链路已经运行并通过验收。
- HTTP TraceId、日志关联、Redis 会话登录与接口权限已实现；Kafka、限流和 Kubernetes 尚未搭建，集中追踪后台和消息追踪也未实现。
- 学习过程中发现代码或配置需要改动时，在代码搭建任务中执行并回写日志；学习任务继续负责解释和复盘。

## 新学习任务的建议开场

可以在新任务中这样说：

```text
请阅读 E:\工作\面经\NovaCloud\学习文档.md，从尚未学习的下一环开始带我理解。一次只讲一个环节，结合项目里的真实配置、代码和运行日志，不要一次讲太多。只负责教学；如果需要修改代码，先告诉我应该回到代码搭建任务做什么。
```
