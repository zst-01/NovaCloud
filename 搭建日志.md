# 搭建日志

> 本文件记录 MicroArchitecture-Lab 的实际搭建过程，面向学习和复盘。原始需求保留在《计划.md》中。
> 每次实际操作后更新；失败、未验证事项和后续计划均单独标明。不得将预期效果记作已验证结果。

## 2026-09-06｜需求沟通与首次环境检查

### 已确认的目标和协作方式

- 通过最小业务复现真实工作项目的基础设施和请求调用链。
- 由助手负责全程搭建，用户通过过程日志了解实现和实验结果。
- 当前仍处于沟通和准备阶段，尚未部署服务。
- 路线：先使用 Docker 在本机跑通，再将同一套服务迁移到本机 Kubernetes。
- 用户确认电脑内存为 32GB；该数值来自用户说明，未通过系统接口核验。

### 用户描述的真实项目结构

前端请求 → Nginx → 应用层网关 → 应用层服务 → 中台层网关 → 中台层服务 → MySQL。

- 应用层网关和应用层服务注册在同一个 Nacos 命名空间。
- 中台层网关和中台层服务注册在另一个 Nacos 命名空间。
- 应用层服务基本负责中转，中台层服务负责业务处理和数据库访问。
- 根据用户回忆，应用层服务通过部署配置提供的固定内网 URL 访问中台网关，不通过 Nacos 跨命名空间发现中台网关。
- 内网域名的实际名称、解析方式和 Kubernetes 配置位置尚未查证，不能当作已确认事实。
- 网关到同层服务是否使用 Nacos 动态发现，尚未核对原项目配置；实验拟采用该方式。

### 第一阶段约定范围

- 使用 Docker Compose 管理本机实验组件。
- Nginx、应用层网关、应用层服务、中台层网关、中台层服务、Nacos、MySQL。
- 一套 Nacos 中创建两个命名空间；无需因为两个命名空间部署两套 Nacos。
- 一个简单查询接口；应用层直接转发，中台层查询 MySQL。
- 应用层通过可配置的中台网关 URL 调用，保留真实项目中的访问边界。
- 每个服务先运行一个副本，面向低流量学习实验。
- Redis、Kafka、鉴权、限流和 TraceId 在基础链路验证后逐步加入。
- Kubernetes 属于后续阶段。

### 本次实际做了什么

1. 阅读《计划.md》，识别原始目标、组件清单和调用链；未修改原文件。
2. 查看项目目录：仅发现《计划.md》，尚无代码或部署配置。
3. 检查上级目录中的 AGENTS.md：未发现适用于当前目录的文件。
4. 检查开发工具：找到 Java、Docker、Git；当前命令搜索路径中未找到 Maven。
5. 执行 Docker 版本和 Compose 版本检查。
6. 检查 Git 状态：当前目录尚不是 Git 仓库。
7. 创建本日志，记录双方约定和实际检查结果。

### 检查结果与限制

| 检查项 | 结果 | 含义 |
| --- | --- | --- |
| Java | 路径为 D:\Dev-tools\JDK\jdk-8u261\bin\java.exe | 发现本机 JDK 8 路径，尚未选定项目 Java 版本 |
| Maven | 当前命令搜索路径中未找到 | 不代表电脑上一定未安装；后续可使用容器构建或 Maven Wrapper |
| Docker CLI | 28.2.2 | 只确认客户端存在 |
| Docker Compose | v2.37.1-desktop.1 | 只确认 Compose 工具可调用 |
| Docker 后台连接 | Access is denied | 当前执行环境无权访问 Docker 管道，不能据此判定 Docker 未启动或损坏 |
| Docker 配置读取 | Access is denied | 未读取配置内容，也未修改权限 |
| Kubernetes 命令行 | 之前检查找到 kubectl | 不等于已有可用的 Kubernetes 集群 |
| WSL 查询 | 之前检查被拒绝访问 | 尚未核实发行版及运行状态 |
| 内存系统查询 | 之前检查被拒绝访问 | 使用用户提供的 32GB 作为规划依据 |
| Git | 当前目录不是 Git 仓库 | 尚未初始化或提交 |

本次没有安装软件、启动容器、创建数据库、修改系统设置或生成业务代码。

### 后续搭建时的记录规则

每一项有意义的操作记录：

1. 目的：这一步解决什么问题。
2. 操作：新增或修改了哪些文件、配置、依赖、容器或数据。
3. 原理：它在完整请求链路中起什么作用，使用简短中文说明。
4. 验证：使用什么请求或检查，实际观察到什么结果。
5. 问题：失败信息、定位依据、修复措施和剩余限制。
6. 复现与回退：必要的启动、停止或恢复方式；涉及数据删除时明确说明。

不记录密码、令牌和其他凭据明文。部署日志与服务运行日志分开：本文件说明搭建过程，服务日志用于运行时排错。

### 下一步

确认进入实际搭建阶段后，先解决 Docker 后台访问验证，确定兼容的 Java/Spring/Nacos 版本，再实现最小链路。完整验证应包括：从 Nginx 入口查询到 MySQL 初始化的数据、两个命名空间注册情况、跨层 URL 调用方式，以及服务不可用时可识别的错误响应。

## 2026-09-06｜开始搭建：Docker 后台验证

- 用户明确授权开始实施第一阶段。
- 使用获准的 Docker 后台访问权限重新检查，确认 Docker Desktop 4.42.1、Engine 28.2.2、Linux 容器后台正常。此前拒绝访问源于当前执行权限限制，并非已证明的 Docker 故障。
- Docker 可见 16 个 CPU、约 15.5GiB 内存；该数值是 Docker 环境可用资源，不是主机总内存。
- 现有 cli-proxy-api 容器正常运行；本项目使用独立 Compose 项目名和端口。
- 已创建 docs/第一阶段实施方案.md，将会话中已批准的架构、验收标准和实施清单落盘。

## 2026-09-06｜最小链路代码和验收脚本

- 已先写 scripts/verify.ps1，并在实现前运行：因 18080 入口未提供查询能力而失败，符合尚未搭建的预期。
- 创建四个独立 Maven 模块、Nginx 配置、MySQL tickets 初始化 SQL、Compose 部署文件。
- 采用 Java 17、Spring Boot 3.2.12、Spring Cloud 2023.0.3、Spring Cloud Alibaba 2023.0.3.2、Nacos 2.4.3 的固定学习基线；官方参考：https://github.com/alibaba/spring-cloud-alibaba/releases/tag/2023.0.3.2 。这是复现用版本组合，不宣称为最新生产推荐版本；兼容性还需以实际构建和联调验证。
- MySQL 使用 8.4.4，Nginx 使用 1.28-alpine；镜像首次下载进行中。
- 本地入口 18080、Nacos 18848、MySQL 13306 均绑定 127.0.0.1。
- .env 保存随机生成的实验数据库密码，已加入 .gitignore 和 .dockerignore，日志不包含密码。
- scripts/start.ps1 负责容器内编译测试、镜像构建、基础组件启动、两个命名空间初始化、应用启动和验收。
- 各层响应增加 X-Lab-* 演示标记，用于学习和验收；它们不是分布式 TraceId，也不是安全证明。

## 2026-09-06｜基础设施启动成功

- 五种基础镜像下载成功：Maven/JDK、Nacos、MySQL、Nginx、Java 运行环境。
- Compose 配置语法检查通过，创建 novacloud-lab_default 网络及项目专用 mysql-data、nacos-data、maven-cache 卷。
- MySQL 和 Nacos 启动且健康检查通过；MySQL 健康检查实际查询 tickets 表，不仅检查端口。
- 运行 scripts/init-nacos.ps1，成功创建 application、platform 命名空间；通过 Nacos API 回读验证。
- Java 首次依赖下载和编译进行中，尚未宣称完整链路可用。
- 新增 8 个针对业务接口和代理行为的自动测试，覆盖数据库取值、404、400、数据库失败、正常转发、上游 404、连接失败 502 和超时 504。
- 新增 README.md，解释 Docker DNS 和 Nacos 服务发现的区别、数据卷保留机制、运行和停止方式。

## 2026-09-06｜网关启动失败定位与修复

- 首轮 Maven 编译成功，8 个业务/代理测试通过；四个运行镜像构建成功。
- 实际启动中，中台服务健康，但中台网关启动失败。
- 读取容器日志确认根因：配置类 RequestLog 默认 Bean 名 requestLog，与同名 @Bean 方法冲突。
- 为两个网关增加 RequestLogTest；修复前测试实际失败，复现 Bean 名冲突。
- 将过滤器工厂方法重命名为 requestLoggingFilter，保留 Spring 的禁止 Bean 覆盖默认行为。正在重编译并回归验证。

## 2026-09-06｜首次完整链路验收及故障实验

- 修复后全部 10 个自动测试通过（两个网关各 1 个、应用层 4 个、中台层 4 个），Maven BUILD SUCCESS。
- 四个 Java 镜像重建成功；7 个运行容器全部健康。
- scripts/verify.ps1 首次完整通过 19 项检查：入口 200、MySQL 工单内容、五层响应标记、404/400、两个命名空间各两个服务及对应健康实例。
- 临时停止中台网关进行故障实验，实际入口返回 504 / platform_timeout。原实验断言只接受 502，因此实验断言失败；finally 已执行恢复。
- 分析：服务停止后 Docker 网络可能表现为连接超时，而非立即拒绝；代码按真实异常分别返回 502（连接失败）或 504（超时），本次观察符合超时分支。不能把“停容器一定返回 502”写作固定结论。
- 新增 scripts/verify-failure.ps1，校验状态码与错误字段的对应关系，并在 finally 恢复中台网关，随后重跑完整验收。未修改代码来强行隐藏 504。

## 2026-09-06｜第一阶段完成与交付

### 最终验证

- Maven 四模块构建成功，10 个自动测试全部通过，0 失败、0 错误、0 跳过。
- scripts/verify.ps1 共 19 项检查全部通过。
- scripts/verify-failure.ps1 完整运行通过：先验收正常链路，停止本项目中台网关，观察 504 / platform_timeout，再自动恢复并重新验收通过。
- 实际运行 scripts/start.ps1 -SkipBuild，通过基础设施健康检查、重复命名空间初始化、应用健康检查和全部链路检查，证明日常入口脚本可重复运行。
- 7 个长期运行容器均健康，服务保持运行供用户访问。
- 实際查看各层日志，可见两个网关收到请求、应用层中转、中台层查询 MySQL，以及 Nginx 入口记录。
- 既有 cli-proxy-api 容器仍在运行，本次未修改或停止它。

### 交付文件

- README.md：架构解释、访问入口、启动停止和数据保留方式。
- docs/第一阶段实施方案.md：完成的实施清单及验收标准。
- pom.xml 及四个模块：Java 17 微服务代码、路由和 10 个自动测试。
- Dockerfile、compose.yaml：容器化构建产物、服务和资源设置。
- infra/nginx/nginx.conf、infra/mysql/001-init.sql：入口代理和数据库初始化。
- scripts/start.ps1、init-nacos.ps1、verify.ps1、verify-failure.ps1：启动、初始化、验收和故障实验。
- .env：本机随机数据库凭据；不输出到日志、不进入构建上下文或未来 Git 提交。

### 当前访问方式

- 完整查询：http://127.0.0.1:18080/api/tickets/1
- Nacos 控制台：http://127.0.0.1:18848/nacos/
- 数据库：127.0.0.1:13306 / novacloud / lab，密码见本地 .env。
- 普通停止：在项目目录执行 docker compose down，数据卷保留。
- 下次启动：PowerShell 7 执行 ./scripts/start.ps1 -SkipBuild。
- 改过 Java 代码后：执行 ./scripts/start.ps1，重新编译测试并构建镜像。

### 边界与后续

第一阶段最小链路已完成。Redis、Kafka、鉴权、限流、完整 TraceId、Kubernetes 尚未搭建；按约定留待后续逐项实验。当前未初始化 Git 仓库，原始《计划.md》保持不变。下一次学习可先从一个实际查询请求出发，对照日志观察每层职责。

## 2026-09-06｜处理 PowerShell 脚本执行策略错误

- 用户在当前终端运行 `./scripts/start.ps1` 时收到 PSSecurityException，提示系统禁止运行脚本。
- 检查确认 Windows PowerShell 5.1 的策略均为 Undefined，最终使用默认 Restricted；PowerShell 7.6.5 已安装，其 LocalMachine 策略为 RemoteSigned。
- start.ps1 只有普通数据流，没有来自互联网的 Zone.Identifier 标记；问题不是脚本内容或下载文件锁定。
- 采用 `pwsh.exe -NoProfile -File .\scripts\start.ps1 -SkipBuild` 从当前窗口显式调用 PowerShell 7，不修改 Windows 全局或用户级执行策略。
- 实际运行退出码为 0：7 个容器健康，两个 Nacos 命名空间就绪，19 项完整链路检查全部通过。
- README 已补充发生此错误时的准确命令，以及首次构建和跳过构建两种用法。

## 2026-09-06｜拆分代码搭建与学习任务

- 根据用户决定，当前任务继续用于搭建项目、修改代码和验证运行结果；另开任务用于逐步学习。
- 新增《学习文档.md》，整理真实项目背景、当前实验结构、第一课内容、自检题和后续学习顺序。
- 文档明确标记当前已完成范围与尚未搭建内容，并提供新学习任务的建议开场文字，减少上下文丢失。
- README 文件导读已增加《学习文档.md》入口。

## 2026-09-06｜开始实现 TraceId 和请求日志

- 用户批准按计划先做 TraceId；本次不进入 Redis、登录鉴权和 Kafka 阶段。
- 读取现有文件，发现 compose.yaml 已有学习用中文注释，修改时保留这些注释和当前业务设置。
- 起始检查 7 个容器均健康。新建 docs/TraceId实施方案.md，明确生成、传播、日志和验收规则。
- scripts/verify-trace.ps1 在旧环境实际失败：响应没有 X-Trace-Id。新增网关测试也在修复前失败，证明当前缺少响应编号和非法编号处理。
- Nginx 生成 X-Trace-Id，覆盖外部传入值，沿链路转发；统一返回一个响应头并输出 JSON 访问日志。
- 网关改用 WebFilter，覆盖错误和未匹配路由，并在响应提交时记录状态与耗时。响应式链路不使用线程 MDC。
- MVC 服务新增 RequestTraceFilter，记录请求并在 finally 恢复 MDC；应用层 RestClient 自动透传当前编号。
- 添加 Docker 日志轮转、按编号查询脚本、并发及故障验收脚本；启动脚本补上 Nginx 配置校验和重载。
- 当前为实现完成待验证，不把测试计划写成通过结果。

## 2026-09-06｜TraceId 与请求日志验收完成

### 实际完成
- 四个模块容器内 clean verify 成功：23 个 Java 测试，0 失败、0 错误，包括既有业务、网关并发、非法编号、Servlet 异常清理和 RestClient 透传检查。
- 新镜像部署成功，Nginx 配置检查及重载成功；7 个容器健康。MySQL 和 Nacos 没有重建。
- 启动脚本通过原 19 项链路检查和 11 个请求的 TraceId 验收，其中包括 6 个并发查询。
- 故障模式通过 12 个请求的 TraceId 验收。中台网关停止时返回 504，对应日志只存在于 Nginx、应用层网关、应用层服务，符合真实经过的路径；网关自动恢复后原 19 项检查再次通过。
- 正常业务查询、工单不存在、参数错误、Nginx 未匹配路径、网关未匹配路径均验证了对应编号和状态码；每个请求在每个实际经过环节只有一条完成日志。
- show-trace.ps1 实测可检索五层完成日志和带 MDC 的中转、MySQL 查询业务日志。示例编号 199fd879842656e1ce9d29efe813ae39（记录于 2026-09-06 15:02:34 UTC；旧运行日志可能随后轮转或随容器重建被移除）。
- 给正常查询附带测试 Authorization 和查询参数，核对该请求日志没有包含测试敏感标记；没有输出真实凭据。
- Docker inspect 确认 Nginx 和四个 Java 容器实际使用 json-file，max-size=10m、max-file=3。本次核对配置生效，没有人为写满磁盘测试轮转。

### 文档与运行入口
- docs/TraceId学习与操作.md：如何拿响应编号查日志，MDC 与响应式处理区别，错误链路、计时单位及日志保存边界。
- scripts/show-trace.ps1：按编号查询最近一段时间的五层日志。
- scripts/verify-trace.ps1：常规 TraceId 验收；-IncludeFailure 增加中台网关停机和自动恢复。
- README.md、学习文档.md 已更新；代码实现进度与用户学习进度分开记录。
- 当前完成的是 HTTP 日志关联，尚无 span、集中日志平台或 Kafka 消息追踪。下一阶段按约定部署 Redis，再实现登录会话和鉴权。

## 2026-09-06｜开始部署 Redis

- 用户确认继续，当前阶段只部署 Redis 并验证 Java 连接，为后续登录会话提供基础。
- Redis 镜像已下载，按实际取得的 SHA256 固定 redis:7.4-alpine 镜像内容。
- Compose 增加 Redis 容器、127.0.0.1:16379 端口、独立数据卷、AOF everysec、128MB 数据内存限制和 512MB 容器限制，使用 noeviction 策略。
- init-env.ps1 为现有 .env 追加随机 Redis 密码，保留 MySQL 凭据；源码和日志不包含密码明文。
- 中台服务增加 Spring Data Redis 依赖、连接超时、密码配置及内部 Redis 健康检查；不新增公开读写接口。
- 新增真实 Redis Java 集成测试及 verify-redis.ps1，覆盖认证、读写、TTL、持久化配置与可选正常重启。
- 启动顺序调整为先启动基础设施，再运行依赖真实 Redis 的 Java 测试；本次尚在验证中。

## 2026-09-06｜Redis 部署与连接验收完成

- Maven 容器内完整 clean verify 成功：24 个 Java 测试，0 失败、0 错误、0 跳过；其中 RedisIntegrationTest 实际连接 Redis，验证写入、读取和 TTL 过期。
- 中台服务镜像已重新构建并部署；start.ps1 -SkipBuild 成功执行，原 19 项链路检查、11 个请求的 TraceId 验收及 Redis 常规验收全部通过。
- Redis 实际版本为 7.4.11；未认证连接被拒绝，认证 PING 成功，AOF everysec 和 noeviction 配置检查通过。
- verify-redis.ps1 -Restart 实际执行一次正常重启，临时测试数据保留，中台 Java Redis 健康状态恢复为 UP，测试 key 已清理。这不代表验证了异常断电的数据安全性。
- 重启后再次运行原 19 项链路检查，全部通过；docker compose ps 确认本项目 8 个容器全部健康。
- 新增 docs/Redis学习与操作.md，README.md 和学习文档.md 已补充 Redis 入口、部署位置及当前业务边界。
- 本阶段未实现登录、鉴权或 Kafka。下一阶段实现 Redis 会话登录、退出和网关鉴权。

## 2026-09-06｜开始实现登录与鉴权

- 用户确认继续登录、退出和网关鉴权阶段。先保存 docs/登录鉴权实施方案.md，确定 MySQL BCrypt 账号、Redis 30 分钟随机 token 会话、两层网关及中台服务权限校验。
- 新验收脚本在旧运行环境失败：未登录查询期望 401，实际 200，确认保护尚不存在。
- 添加演示账号初始化、登录/当前用户/退出接口，应用层透传 Authorization；两层网关使用响应式 Redis，Redis 错误时返回 503。
- .env 只追加随机 DEMO_PASSWORD，未输出凭据或重置既有数据库数据。启动中台会幂等创建 lab_users 表并补充缺失的 demo、observer 账号。
- Java 构建测试已通过；正在部署并执行真实 HTTP、Redis 过期、权限绕过及故障验收。最终结果以下续条目为准。
- 实际 TTL 验收首次失败，使用非敏感 sample 验证 Windows 管道末尾含 0d 回车，导致容器读取错误 key；在脚本标准输入处理处移除 CR/LF 后，真实 30 分钟 TTL 和过期验证通过。
- Redis 停机实验已返回 503；恢复全部容器健康后，原会话查询成功，退出后返回 401。没有删除数据卷或清空 Redis。
- 自查时将网关和服务入口改为默认校验、精确健康检查白名单，补充编码路径、矩阵参数和健康路径前缀回归测试，防止按原始路径匹配时出现绕过。

## 2026-09-06｜登录与鉴权验收完成

- 最终 Java verify 成功：46 个测试，0 失败、0 错误、0 跳过。覆盖 BCrypt 密码及账号幂等初始化、随机会话、登录/当前用户/退出、权限检查、Redis 错误、特殊路径，以及已有业务和 TraceId 测试；真实 Redis 集成测试已启用。
- 最终镜像部署完成，start.ps1 -SkipBuild 返回成功：原 19 项链路检查、11 个请求（含 6 个并发）的 TraceId 验收、Redis 部署检查和登录鉴权脚本均通过。
- 真实 HTTP 验证：缺少或伪造 token 为 401，错误密码为 401，缺少登录字段为 400；demo 登录及查询为 200，observer 查询为 403，伪造角色头无法提权。
- 真实 Redis 验证：新会话 TTL 接近 1800 秒；仅将验收脚本自己的新会话缩短为 1 秒后，请求返回 401；退出后立即返回 401，TTL=-2 确认当前会话 key 已删除。
- 两层网关及中台权限测试通过；直接请求中台网关/中台服务的匿名访问被拒绝，无权限账号也被拒绝。编码路径、矩阵参数及健康路径前缀检查通过。
- Redis 停机实验返回 503，自动恢复后原会话仍可查询；中台网关停机实验返回 504 / platform_timeout，自动恢复后原 19 项链路检查全部通过。
- 拒绝请求的完成日志只出现在 Nginx 和应用网关；扫描本次运行日志，没有发现本次验收会话 token 或 DEMO_PASSWORD。凭据未输出，测试会话已清理。
- 最终 docker compose ps 确认 8 个容器全部 healthy；所有 PowerShell 脚本语法检查通过。
- 已提供 docs/登录鉴权学习与操作.md，并同步 README、学习文档、Redis 和 TraceId 操作说明。当前仅有 API，没有登录页面；限流、Kafka 和 Kubernetes 留待后续。
